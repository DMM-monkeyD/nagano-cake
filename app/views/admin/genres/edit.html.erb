<div class="container mt-5">
  <div class="row"><h1>ジャンル編集</h1>
  </div>
  <div class="row　mt-5">
    <p>ジャンル名</p>
    <p><%= form_with model: @genre, url: admin_genre_path, method: :patch, local:true do |f| %>
         <%= f.text_field :name %>
         <%= f.submit '変更を保存' %>
       <% end %>
    </p>
  </div>
</div>
